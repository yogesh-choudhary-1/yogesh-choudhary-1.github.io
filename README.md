# My PC website

One-page static landing site for My PC.

Upload `index.html` and `style.css` to a GitHub Pages repository. Replace the placeholder GitHub URL in `index.html` with your actual repository URL before publishing.
